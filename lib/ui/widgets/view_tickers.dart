import 'package:flutter/material.dart';

class ViewTickers extends StatefulWidget {
  const ViewTickers({Key? key}) : super(key: key);

  @override
  _ViewTickersState createState() => _ViewTickersState();
}

class _ViewTickersState extends State<ViewTickers> {
  @override
  Widget build(BuildContext context) {
    return Column(

    );
  }
}
